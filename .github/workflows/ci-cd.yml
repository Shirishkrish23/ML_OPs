name: CI/CD Pipeline

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: "3.9"

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run Tests
        run: |
          # Add your test commands here
          # For example, if using pytest:
          pytest

      - name: Build Docker Image
        run: |
          docker build -t your_dockerhub_username/movie-recommender:$GITHUB_SHA .

      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.shirishkrishna }}
          password: ${{ secrets.dckr_pat_bSoMf48xbV1y9QFI9E6GNXL2XDM}}

      - name: Push Docker Image
        run: |
          docker push your_dockerhub_username/movie-recommender:$GITHUB_SHA
